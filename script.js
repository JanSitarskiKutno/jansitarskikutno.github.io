let a = document.getElementById("skill");

function skills(x) {

    switch (x)
    {
        case 1:
            a.innerHTML = "C++ uczę się od samego początku mojej nauki programowania, "
            break;
    }
}